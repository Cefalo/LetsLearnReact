import React, {Component} from 'react';
import PropTypes from 'prop-types';

const SearchBar = () => {
	return (
		<div>Search Bar</div>
	);
}

SearchBar.propTypes = {
    
}

export default SearchBar;
